import React from 'react';
import Head from './Heading';

const SideNav = () => {
    return(
        <nav className="sidebar side-nav">
            <Head />
        </nav>
    )
}

export default SideNav;